function solution(slice, n) {
    //피자 조각 수 slice와 피자를 먹는 사람의 수 n이 매개변수로 주어질 때, n명의 사람이 최소 한 조각 이상 피자를 먹으려면 최소 몇 판의 피자
    // 7 , 10 => 2
    // 4 , 12 => 3
    return n % slice == 0 ? Math.floor(n / slice) : Math.floor(n / slice) + 1;
}
